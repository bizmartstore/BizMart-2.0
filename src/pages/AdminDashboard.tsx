-      notifyCustomerGCashComplete(tx.user_id, tx.type, tx.amount, status); // <-- TypeScript compiler error here
+      notifyCustomerGCash(tx.user_id, tx.type, tx.amount, status); // assuming this is the correct function