To deploy the Edge Functions and ensure all secrets are properly configured:

1. Open your Vercel project dashboard
2. Navigate to Settings > Environment Variables
3. Add the following variables (all required):

   - TELEGRAM_BOT_TOKEN: 8646984516:AAHAEuEReopFP79J05Rs7De1Ormd7NuVWz0
   - TELEGRAM_CHAT_ID: -1003171423988
   - ONESIGNAL_APP_ID: [Your OneSignal App ID from Supabase]
   - ONESIGNAL_REST_API_KEY: [Your OneSignal REST API Key from Supabase]
   - SUPABASE_URL: [Your Supabase Project URL]
   - SUPABASE_SERVICE_ROLE_KEY: [Your Service Role Key from Supabase]
   - SUPABASE_ANON_KEY: [Your Anon Key from Supabase]

4. For Edge Functions, also ensure:
   - VITE_ONESIGNAL_APP_ID is set (used in OneSignalInit.tsx)
   - All Supabase URLs and keys are correctly set

5. Deploy the project
6. After deployment, test notifications by performing actions that trigger them (order placement, print requests, etc.)

Important Notes:
- Edge Functions run on Deno 1.x runtime- Environment variables must be prefixed with NEXT_PUBLIC_ if accessed from client-side code
- For server-side only variables (like TELEGRAM_BOT_TOKEN), no prefix is needed
- After adding environment variables, you must redeploy the project for changes to take effect

Testing Tips:
- Use browser console to check for OneSignal initialization errors
- Use Supabase Dashboard > Table Editor to verify table structures
- Check Vercel Function Logs for any Edge Function errors
- Test on actual mobile devices to ensure push notifications work